services:
  # LinkedIn Application (FastAPI Worker)
  application:
    build: .
    container_name: LinkedIn-Post
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    env_file:
      - .env
    command: uvicorn core:app --reload --host 0.0.0.0 --port 8000